spring:
  datasource:
    url: jdbc:h2:mem:tasks;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update

security:
  jwt:
    secret: "dev-only-secret-change-me-please-dev-only-secret"
    ttlSeconds: 3600

server:
  port: 8080
