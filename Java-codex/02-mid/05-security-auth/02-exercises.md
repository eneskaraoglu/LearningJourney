# Exercises

1. Configure a security chain that permits `/auth/**` and protects `/api/**`.
2. Implement a login endpoint that returns a JWT.
3. Add method-level security with `@PreAuthorize`.
4. Configure password encoding with BCrypt.
5. Explain CSRF protection for browser apps vs APIs.
6. Add a role-based rule for admin endpoints.
