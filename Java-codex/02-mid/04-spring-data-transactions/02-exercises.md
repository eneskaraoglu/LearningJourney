# Exercises

1. Explain when `REQUIRES_NEW` is appropriate.
2. Model a money transfer with atomic updates to two accounts.
3. Add `@Version` for optimistic locking in an entity.
4. Demonstrate a lost update and how to prevent it.
5. Compare `READ_COMMITTED` and `REPEATABLE_READ`.
6. Add a batch insert method that avoids memory spikes.
