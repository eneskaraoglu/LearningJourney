# Mid Deep Exercises

1. Add profile-based configuration and secrets loading (mocked) to a Boot app.
2. Implement integration tests using `@SpringBootTest` and H2.
3. Design a transactional money transfer with optimistic locking.
4. Add JWT auth with refresh tokens and test the filter chain.
5. Add Redis cache with TTL for a hot endpoint (mocked in code).
6. Build a Kafka-style event processor with idempotency handling (simulate with queue).
7. Add structured logging with correlation IDs and verify in tests.
8. Add metrics with Micrometer timers for request latency.
