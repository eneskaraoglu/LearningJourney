# Solutions (Short)

1. Use `LinkedHashSet` to preserve order, then convert to list.
2. Use `Map<String, Integer>` and `getOrDefault`.
3. Count with `LinkedHashMap`, then scan for value `1`.
4. `class Box<T> { private T value; ... }` with getters and setters.
5. Use `new HashSet<>(list)`.
6. `LinkedHashMap` with access order and eviction logic.
