# Exercises

1. Create a Spring Boot app with a `/health` endpoint.
2. Add a `Task` model and implement CRUD endpoints.
3. Split logic into controller, service, and repository layers.
4. Add `application-dev.yml` and `application-prod.yml`.
5. Validate input using `@Valid` and `@NotBlank`.
6. Return proper HTTP status codes for errors.
