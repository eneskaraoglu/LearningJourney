# Exercises

1. Read a text file and count lines, words, and characters.
2. Parse a CSV file into a list of `User` objects.
3. Create a `ValidationException` for invalid email format.
4. Write a method that copies a file using `Files.copy`.
5. Handle `NumberFormatException` in a safe parsing utility.
6. Add logging when a file is missing instead of returning `null`.
